apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: breakstatementhrmsoracleapppvc
spec:
  resources:
    requests:
      storage: "1Gi"
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
